#[macro_use]
mod console_log;
pub mod error;
// mod game_wrapper;  // Commented out - depends on missing ogame_core crate
// mod socket;        // Commented out - depends on missing pharos and ws_stream_wasm crates
pub mod download_utils;

// pub use game_wrapper::{game, game_mut, GameWrapper, GAME_WRAPPER};
// pub use socket::Socket;
